{
  "mcpServers": {
    "crumb": {
      "command": "python3",
      "args": ["${CLAUDE_PLUGIN_ROOT}/mcp/server.py"],
      "description": "CRUMB format tools — create, validate, lint, search, palace memory, governance."
    }
  }
}
